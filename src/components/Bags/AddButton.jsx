import {
	Button,
	Dialog,
	DialogActions,
	DialogContent,
	DialogContentText,
	DialogTitle,
	TextField,
} from "@mui/material";
import { use, useState } from "react";
import { api } from "../../api";
import { Url } from "../../constants";
import { useNavigate } from "react-router";

function AddButton() {

    return(
        <>
            <h1>Test</h1>
			<script>console.log("pop up loaded")</script>
        </>
    );
}
export default AddButton;